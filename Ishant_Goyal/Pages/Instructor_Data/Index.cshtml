@page
@model Ishant_Goyal.Pages.Instructor_Data.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Instructor[0].InstructorName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Instructor[0].JoiningDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Instructor) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InstructorName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JoiningDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.InstructorId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.InstructorId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.InstructorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
